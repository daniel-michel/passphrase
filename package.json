{
  "scripts": {
    "build-prod": "npm run build -- --sourceMap false --declaration false --removeComments true",
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "cd dist && git clean -dfX"
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "lit": "^2.7.4"
  }
}
